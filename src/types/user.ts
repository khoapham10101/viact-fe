export interface User {
  createdAt: string;
  email: string;
  firstName: string;
  id: string;
  isVerify: boolean;
  lastName: string;
  phoneNumber: string | null;
  updatedAt: string;
  username: string;
}
